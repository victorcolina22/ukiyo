export enum RelationshipType {
  Artist = "artist",
  Author = "author",
  CoverArt = "cover_art",
  Creator = "creator",
}

export enum Group {
  Format = "format",
  Genre = "genre",
  Theme = "theme",
}
export enum TagType {
  Tag = "tag",
}
